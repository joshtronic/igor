[Unit]
Description=Tick timer — fires the next tick

[Timer]
# First tick a couple of minutes after boot.
OnBootSec=2min
# Subsequent ticks measured from previous completion — prevents overlap
# if a tick runs long. Override via a drop-in at
# ~/.config/systemd/user/tick.timer.d/override.conf
OnUnitInactiveSec=10min
Persistent=true

[Install]
WantedBy=timers.target
